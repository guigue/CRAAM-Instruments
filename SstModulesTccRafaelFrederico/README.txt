## FOLLOW THE INSTRUCTIONS BELOW INSIDE "sttModules" DIRECTORY

# 1: You need to install all dependencies before run the program, please run the commands:
sudo apt-get update
sudo apt-get -y upgrade
sudo apt-get install -y python3-pip
pip3 install -r requirements.txt

# 2: After that, run:
python3 __init__.py